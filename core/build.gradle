apply plugin: "java"

sourceCompatibility = 1.6

dependencies{
	compile fileTree(dir: 'libs', include:'*.jar')
}

sourceSets.main.java.srcDirs = [ "src/" ]

eclipse.project {
    name = appName + "-core"
}
